rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    
    // ============================================
    // OPEN PERMISSIONS - DEVELOPMENT/TESTING ONLY
    // WARNING: This allows ALL read/write access!
    // DO NOT USE IN PRODUCTION!
    // ============================================
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
